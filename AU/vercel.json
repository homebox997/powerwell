{
  "buildCommand": "echo 'Static HTML - no build needed'",
  "outputDirectory": ".",
  "installCommand": "echo 'No dependencies - static site'",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
